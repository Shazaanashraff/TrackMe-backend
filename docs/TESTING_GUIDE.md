# Backend Testing Guide

This guide maps backend behaviors to tests and indicates when to update tests.

## Error Handling
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| middleware/errorHandler | unit (no DB) | tests/unit/errorHandler.test.js | a Mongoose `CastError` returns a clean 400 without echoing the raw invalid value; an unhandled (statusCode 500) error's message is masked to a generic string when `NODE_ENV=production`, but shown as-is outside production; a deliberate `ApiError` (statusCode < 500) always shows its own message, even in production | issue #65 — the production message-masking rule, or the CastError branch, changes |

## Auth
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| middleware/auth.js role gates: requireManagerOrAbove, requireManager, requireSuperAdmin, requireRoles | unit (no DB) | tests/unit/auth-roles.test.js | requireManagerOrAbove admits 'admin' (manager) and 'super-admin', rejects driver/user; requireManager admits only 'admin', rejects 'super-admin'; requireSuperAdmin admits only 'super-admin'; requireRoles rejects an unauthenticated request with 401 | issue #78 — `requireAdmin` renamed to `requireManagerOrAbove` to stop it reading as "admin-role-only" when it actually means manager-or-above; the role matrix or gate names change again |
| POST /api/auth/register + POST /api/auth/verify-email | integration | tests/integration/auth.test.js | register → unverified user + `requiresVerification` + `developmentOtp` (Resend mocked, never hits the real API); verify-email wrong OTP → 400; correct OTP → verified + tokens; duplicate email → 409 `code: 'EMAIL_IN_USE'` with `canSignIn: true` when the submitted password matches the existing rider account, `canSignIn: false` on a wrong password or a Manager-account email match | register/verify contract, OTP flow, verification email template, or duplicate-email/`canSignIn` logic changes |
| POST /api/auth/login | integration | tests/integration/auth.test.js | valid creds → 200 + tokens, invalid creds → 401, missing password → 400, unverified account → 403 with `requiresVerification` + `email` | auth flow changes |
| POST /api/auth/login with a driver ID | integration | tests/integration/manager-drivers.test.js | driver code signs in dashed or bare/lower-case; wrong password → 401 with a driver-ID-worded message; a driver who has an email can still sign in with it; `user.driverCode` returned | login identifier handling or the driver-code format changes |
| driverCode utils: generateDriverCode, normalizeDriverCode, looksLikeDriverCode | unit (no DB) | tests/unit/driver-code.test.js | DRV-XXXX-XXXX shape, no look-alike characters (I/O/0/1), uniqueness across draws; normalizes case/spacing/missing prefix; rejects partials and anything containing `@` | driver-code format or normalization rules change |
| POST /api/auth/refresh-token | integration | tests/integration/auth/refresh.test.js | valid, invalid | token lifecycle changes |
| POST /api/auth/forgot-password/request-otp + verify-otp | integration | tests/integration/password-reset.test.js | correct OTP on a fresh request verifies; 5 wrong attempts locks out the code (rejects the correct code afterwards, even unexpired); a fresh request after lockout issues a new usable code; a genuine email-send failure in production returns `success:false` + 502 with no `developmentOtp` (dev/test env unaffected); an unregistered email still returns `success:true`/200 in production (no enumeration signal); reset OTP expires in 5 minutes, shorter than the 10-minute email-verification OTP | otp verification, lockout threshold, reset logic, email-failure handling, or expiry duration changes |
| PUT /api/auth/profile (name + phoneNumber) | integration | tests/integration/auth.test.js | accepts + persists `phoneNumber`, returned on `user`; rejects malformed `phoneNumber`; empty string clears it; 401 when unauthenticated | profile update contract or phoneNumber validation changes |
| GET /api/auth/me | integration | tests/integration/auth-me.test.js | returns the signed-in account in the same shape login does, `phoneNumber` included; reflects a manager's edit made after the token was issued; never returns the password; 401 without a token; 401 on a token signed with a non-allowlisted algorithm (HS384) or `alg: none` — `protect()` pins `algorithms: ['HS256']` on every `jwt.verify` call | the account payload changes, another client needs to re-read itself, or the signing algorithm allowlist changes |
| accountRegistry (findAccountByEmail/findAccountById/isEmailRegistered) | integration | tests/integration/account-registry.test.js | cross-collection lookup by email (Manager/Driver/SuperAdmin/User), role-scoped lookup by id, `select` passthrough, cross-collection email uniqueness incl. `excludeId` | account-schema split changes (new account type added/removed, lookup signature changes) |
| scripts/migrate-account-schemas.js dedup rule | unit (no DB) | tests/integration/migrate-account-schemas.test.js | `selectCanonicalSuperAdmin`: no/one/many super-admins, canonical-email preference, case-insensitive match, earliest-created fallback; `stripRole` | dedup rule or migration script logic changes |
| Rate limiting on POST /api/auth/resend-verification-otp and POST /api/auth/forgot-password/request-otp | integration | tests/integration/auth-rate-limit.test.js | 4th request within the window → 429 for both endpoints; generic 429 message (no email-existence leak); window reset allows a further request; limits are independent per email address | rate-limit threshold/window or the endpoints' throttling logic changes |
| Rate limiting on POST /api/auth/login | integration | tests/integration/auth-login-rate-limit.test.js | 4th attempt within the window for the same identifier → 429 (attempts count regardless of outcome); window reset allows a further attempt; limits are independent per identifier; a real account can still log in through its own unexhausted bucket | issue #35 — login's rate-limit threshold/window, or its identifier-based keying, changes |
| emailRateLimiter.createEmailRateLimiter keyExtractor | unit (no DB) | tests/unit/emailRateLimiter.test.js | defaults to keying by req.body.email; an empty key skips limiting entirely; a custom keyExtractor (used by login's identifier field) buckets independently of req.body.email; the bucket resets once the window elapses | the rate-limiter's key resolution or window/reset logic changes |
| Session revocation on deactivation (`protect` in middleware/auth.js) | integration | tests/integration/session-revocation.test.js | a manager's or super-admin's already-issued, unexpired access token is rejected (403) on the very next request once their account is deactivated — `protect` re-fetches the account from its collection every request rather than trusting the JWT payload | issue #36 — `protect`'s per-request account fetch or its `isActive` check changes |
| `developmentOtp` echo gate (register, resend-verification-otp, forgot-password/request-otp) | integration | tests/integration/password-reset.test.js | with `ENABLE_DEV_OTP_ECHO` unset, the OTP is never echoed even outside production (`NODE_ENV=test`) — a misconfigured/unset `NODE_ENV` alone can no longer leak it; the flag is set repo-wide for the test run via `tests/jest.setup.js` so the rest of the suite (which reads `developmentOtp` directly) is unaffected (issue #66) | `shouldEchoDevOtp()` in authController.js changes, or another call site starts echoing an OTP |

## Rider Profiles (multiple riders per account — docs/modules/PROFILES.md)
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| accountRegistry.loginFilterForRole | unit (no DB) | tests/unit/account-login-filter.test.js | `user` role narrows to `{profileKind:{$ne:'MANAGED'}}`; every other role gets `{}`; an unknown role degrades to `{}` rather than throwing | the login-determinism filter or the account-type list changes |
| scripts/migrate-rider-profiles.js pure planning helpers | unit (no DB) | tests/unit/migrate-rider-profiles.test.js | `planProfileKindBackfill` selects only documents with no `profileKind`; `planBlankEmailCleanup` selects only already-`MANAGED` documents with a blank/null email, never a `PRIMARY` (or about-to-become-`PRIMARY`) one | the migration's planning logic changes |
| User schema invariants: profileKind, email-by-kind, scoped uniqueness | integration | tests/integration/user-profile-schema.test.js | defaults to `PRIMARY`; a second `PRIMARY` on one identity is rejected by the unique index; several `MANAGED` profiles coexist freely; a `PRIMARY` with no email is rejected; a `MANAGED` with an email is rejected; a blank-string email normalises to no email, not a validation error | the `profileKind` enum, the email-by-kind `pre('validate')` hook, or the scoped index change |
| scripts/migrate-rider-profiles.js live run | integration | tests/integration/migrate-rider-profiles.test.js | dry run makes no changes; `--apply` backfills `profileKind` and unsets blank emails only on already-`MANAGED` docs, never a `PRIMARY`; rebuilds the identityId_1_primary_unique / plain identityId_1 / sparse-unique email_1 indexes from a hand-built legacy shape; a second `--apply` is a no-op; `--verify` fails before `--apply` and passes after; `--verify` catches a duplicate PRIMARY that bypassed the guarding index | the migration's write logic or index shape changes |
| middleware/auth.js requireOwnProfile, requirePrimaryProfile | integration | tests/integration/require-own-profile.test.js | admits the caller's own PRIMARY and MANAGED profiles; 404s a different identity's profile, a non-existent id, and a soft-deleted one; 404s when the caller has no identityId; **never matches two profiles that both lack an identityId** (the null-equality regression); requirePrimaryProfile admits only the account holder, 403s a MANAGED profile with `code: 'MANAGED_PROFILE_FORBIDDEN'` | either guard's comparison logic changes |
| GET/PATCH/DELETE/POST /api/profiles, POST /api/profiles/:id/switch, GET /api/profiles/household/enrollments | integration | tests/integration/profiles.test.js | list returns the household + account block; create/update/delete happy paths; 403 `MANAGED_PROFILE_FORBIDDEN` on create/delete from a managed session; 409 `HOUSEHOLD_LIMIT`; 409 `CANNOT_DELETE_PRIMARY`; delete cascades DriverEnrollment but keeps BoardingEvent; switch issues a working token pair without invalidating the source profile's session, 403s a deactivated target; 404 on every mutating route for another identity's profile; household-enrollments groups per profile and returns `[]` (not everyone) for an identity-less caller | any endpoint's contract, status codes, or authz rule changes |
| authController identity hydration for a MANAGED profile | integration | tests/integration/auth-managed-profile.test.js | getMe, updateProfile, updateAvatar, and refresh-token all report the account holder's email (not blank) when the active profile is MANAGED; updateProfile ignores a submitted `phoneNumber` for a MANAGED profile | `hydrateIdentity`/`userPayload` call sites, or the MANAGED phoneNumber guard, change |
| utils/pushHelper.js resolvePushTokensForRider | integration | tests/integration/push-helper-household.test.js | a boarding push for a scanned MANAGED profile reaches the account holder's device; tokens are unioned and de-duplicated across the household; skips delivery when nobody in the household has a device | household push resolution changes |
| notificationController household scoping + device-token target | integration | tests/integration/notifications-household.test.js | reads include every profile's notifications by default; `?profileId=` narrows to one (and returns nothing, not an error, for a profile outside the household); a device token registered from a MANAGED session lands on the PRIMARY profile | `resolveScopedUserIds` or the device-token target changes |
| attendanceController household access | integration | tests/integration/attendance-household.test.js | the account holder reads a managed child's attendance and vice versa; a rider with no relation and no managing role is refused; the null-equality regression case (two identity-less accounts never match) | the household-membership check in `getStudentAttendance` changes |
| managerEnrollmentsController passenger.account for a MANAGED profile | integration | tests/integration/manager-enrollments-managed-profile.test.js | a managed passenger's row carries the owning account's email/phone/name and `isManagedProfile:true`; a PRIMARY passenger's account is itself; the approve/reject response carries the same detail | `requestSummary`/`resolveAccountsForPassengers` changes |
| superAdminController shared-identity email guard vs. managed profiles | integration | tests/integration/manager-shared-identity-email.test.js | a manager-only identity may change its email; a manager who also holds a rider role may not; adding managed children to that identity does not change the count either way | `updateManager`'s `findProfilesForIdentity(...).length` guard changes |
| socket/socketHandler.js household auto-join | ws integration | tests/integration/ws/household-socket.test.js | a connection made with the PRIMARY's token also receives `attendance:event`s for a managed child's `student:<id>` room, and still receives its own | the household room-join logic changes |

## Routes and Buses
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| POST /api/routes | integration | tests/integration/routes-creation.test.js | admin/manager happy path; non-admin and unauthenticated refused; duplicate routeId; missing/invalid required fields; invalid serviceType | route creation contract or admin-role gating changes |
| PUT /api/routes/:routeId, PATCH /api/routes/:routeId/toggle, DELETE /api/routes/:routeId ownership | integration | tests/integration/authz-ownership.test.js | owning manager can edit/toggle/delete; a different manager gets 403 on all three and the route is unchanged; super-admin can edit any route; each owner-triggered mutation writes a `ManagerAuditLog` entry (`ROUTE_UPDATED`/`ROUTE_STATUS_TOGGLED`/`ROUTE_DELETED`) | route ownership rule, `managerId` assignment on create, or audit-log fields change |
| DELETE /api/routes/:routeId unassigns referencing vehicles | integration | tests/integration/delete-route-orphan-vehicle.test.js | deleting a route clears `routeId` off every non-deleted vehicle that referenced it and reports the count as `unassignedVehicles`; an already-deleted vehicle referencing the route, and a vehicle on an unrelated route, are both left untouched; deleting a route with no referencing vehicles reports `unassignedVehicles: 0` | issue #50 — the cascade/unassign behavior on route deletion changes |
| GET /api/routes/stats/overview | integration | tests/integration/route-stats.test.js | totalRoutes/activeRoutes/inactiveRoutes counts and avgDistance/avgEstimatedTime over non-deleted routes (single `$facet` aggregation, not 4 separate queries) | stats calculation or response shape changes |
| POST /api/vehicle/register | integration | tests/integration/vehicle-registration.test.js | driver happy path; non-driver and unauthenticated refused; duplicate vehicleId/numberPlate; unknown routeId; serviceType/route mismatch; missing/invalid required fields | vehicle registration contract or driver-role gating changes |
| PUT /api/vehicle/:vehicleId, PATCH /api/vehicle/:vehicleId/maintenance ownership | integration | tests/integration/authz-ownership.test.js | a different driver/unrelated manager gets 403 on both and the vehicle is unchanged; the owning driver can edit and set maintenance status; `managerId`/`driverId`/`isDeleted` are silently stripped from a non-super-admin's update (owning driver or owning manager) so ownership can't be reassigned through this path; an unrelated manager can't claim an orphaned (`managerId: null`) vehicle; an owning manager's mutation writes a `ManagerAuditLog` (`VEHICLE_EDITED`) entry; a rider (role: user) gets 403 at the route's `requireRoles` middleware, not just controller-internal logic (issue #37) | vehicle ownership rule, the ownership-field allowlist, the route's role gate, or audit-log fields change |
| GET /api/bookings/:bookingId ownership | integration | tests/integration/authz-ownership.test.js | a different rider gets 403/404; the owning rider gets 200 with the booking | booking read authorization changes |
| GET /api/manager/attendance cross-manager scoping | integration | tests/integration/authz-ownership.test.js | a rider who only rode another manager's route never appears in the caller's rollup; an explicit `routeId` for a route the caller doesn't own is 403 | attendance scoping or rollup logic changes |
| GET /api/vehicle/list/all?serviceType= | integration | tests/integration/vehicle-registration.test.js | filters to the requested serviceType; an invalid filter value is ignored (unfiltered results) | serviceType filtering logic changes |
| GET /api/vehicle/routes | integration | tests/integration/list-pagination-cap.test.js | oversized limit clamped to 100; no limit/page keeps the full-list default | pagination/limit logic changes |
| /api/bus/stops + /api/bus/routes/plan | integration + unit | tests/integration/journey-plan.test.js | stop list, direct A→B matching, direction filter, no-match, validation; geo helpers | journey-matching or geo logic changes |
| Page-size caps: GET /api/vehicle/list/all, GET /api/vehicle/routes, GET /api/vehicle-reviews/vehicle/:id | integration | tests/integration/list-pagination-cap.test.js | an oversized requested `limit` is clamped to 100 on all three; no limit/page param keeps each endpoint's pre-existing default (10 for the vehicle list, full list for routes and reviews) | list endpoint pagination or page-size limits change |
| /api/places/* (autocomplete, details, reverse) | integration | tests/integration/places-proxy.test.js | input guards, 503 no-key, no key leak, reverse coord validation, reverse name derivation: Google address_components (road/POI > town) + OSM structured + fallback | proxy guards or reverse-geocode name logic change |
| /api/transit/plan (normalize, group, prune, classify) | integration | tests/integration/transit.test.js | leg extraction, interchangeable-bus grouping, redundant pruning, service classification (local/express/long-distance by line name + stop spacing) + local-first ordering, guards | transit normalisation, classification list, or ordering change |

## Bookings and ETA
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| POST /api/bookings | integration | tests/integration/bookings.test.js | valid booking → 201; missing required fields → 400; unauthenticated → 401; seat conflict → 409; past `journeyDate` → 400; today's `journeyDate` still accepted; partial seat overlap → 409 naming only the taken seat(s), the rest of the request stays bookable; same seat on a different `journeyDate` is not a conflict | booking rules or journeyDate validation change |
| /api/eta/* | integration | tests/integration/user/eta.test.js | ETA calculations | ETA logic changes |

## Notifications
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| DELETE /api/notifications/admin/cleanup | integration | tests/integration/notifications.test.js | 401 no token; 403 rider; 403 driver; 200 manager (deletes expired docs system-wide, leaves non-expired); 200 super-admin | cleanup guard or delete-scope logic changes |
| /api/notifications | integration | tests/integration/shared/notifications.test.js | list, read, delete | notification schema changes |
| /api/notifications/:id, /:id/read, DELETE /:id ownership | integration | tests/integration/notifications.test.js | GET/PUT(read)/DELETE on another user's notification → 404, no mutation; same operations on the caller's own notification succeed | notification ownership/authorization logic changes |
| GET /api/notifications/count/unread | integration | tests/integration/notifications.test.js | returns 0 for a caller with no notifications; scoped to the caller — only counts their own `isRead:false` notifications, ignoring another user's unread and the caller's own read ones | unread-count query or scoping changes |
| pushHelper.sendBoardingPush | unit | tests/integration/push-helper.test.js | no-tokens skip, invalid-token filtering, SDK-error swallowing (never throws) | expo-server-sdk version/API or push payload shape changes |

## QR Attendance
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| qrToken utils: signQr/verifyQr (account-scoped) | unit | tests/integration/qr-attendance.test.js | signQr payload is `{ sub: userId, ver: qrTokenVersion, jti }`; verifyQr resolves valid; rejects garbage/tampered, expired, stale version (after rotate), inactive/missing user | token payload shape or verification logic changes |
| POST /api/qr/issue, POST /api/qr/rotate | integration | tests/integration/qr-attendance.test.js | issue returns one account-scoped token with no route gate (works even with zero route relationships); rotate bumps the caller's own qrTokenVersion, invalidating prior tokens | issue/rotate contract changes |
| POST /api/driver/boarding/scan | integration | tests/integration/qr-attendance.test.js | 403 non-driver; 401 invalid/expired/stale token; 404 bus not assigned to driver; 403 when the bus's route has `qrEnabled:false`; auto-toggle BOARD→ALIGHT; debounces duplicate same-type scan within the window; dispatches push; out-of-range lat/lng discarded (stored as null) instead of the whole scan being rejected; valid lat/lng stored unchanged; debounce boundary — a prior same-type event timestamped 1s inside the window still debounces, 1s outside does not; a rider with events open on two different `tripId`s keeps auto-toggle state independent per trip; an explicit `tripId: null` falls back to the per-vehicle per-day default | scan logic, toggle/debounce, route-gating, coordinate-range, or tripId-scoping changes |
| PATCH /api/manager/routes/:routeId/qr | integration | tests/integration/qr-attendance.test.js | owning manager toggles `qrEnabled` on/off, scan endpoint reflects it immediately; 403 for a manager who doesn't own the route | route QR toggle endpoint or ownership scoping changes |
| GET /api/attendance/student/:studentId, GET /api/manager/attendance | integration | tests/integration/qr-attendance.test.js | rider reads own history + summary; 403 non-manager reading another rider; manager per-student rollup scoped to owned routes; empty rollup for a manager with no routes; an unparseable `from`/`to` query param on either endpoint returns 400 instead of silently rolling up to an empty result (issue #55) | attendance read/authorization or rollup logic changes |
| dateRange utils: resolveRange (shared by attendanceController + managerAttendanceController) | unit | tests/unit/dateRange.test.js | throws `ApiError(400, ...)` on an unparseable `from`/`to` instead of producing an `Invalid Date` that Mongo silently matches nothing against; also now rejects any provided `from`/`to` that lacks an explicit `Z`/UTC-offset suffix (bare date/local-time strings are parsed as UTC by `Date`, silently shifting a non-UTC manager's "today" window — issue #58); a correctly-offset `from`/`to` pair resolves to the exact expected UTC instants | the range-resolution rule changes, the offset requirement is relaxed, or a new endpoint needs the same from/to parsing |
| GET /api/driver/boarding/roster | integration | tests/integration/qr-roster.test.js | enrolled roster (ACTIVE RouteMembership) with per-rider status `ON`/`OFF`/`NOT_BOARDED` from latest trip event; `onBoardCount`/`enrolledCount` headline; sort `ON→NOT_BOARDED→OFF`; on-board non-members surfaced as `guests` (excluded from counts); 400 missing busId; 404 bus not the driver's; 403 non-driver; 403 route `qrEnabled:false` | roster status derivation, enrollment source, guest handling, or authorization changes |
| POST /api/notifications/device-token | integration | tests/integration/qr-attendance.test.js | registers a token idempotently (no duplicates in `pushTokens`); 400 missing token | device-token contract changes |
| `student:<userId>` socket auto-join | ws integration | tests/integration/ws/qr-attendance-socket.test.js | every authenticated connection auto-joins its own student room with no explicit client-side join, so a server-side `attendance:event` emit is received | socket connection/room-join logic changes |

## Driver Earnings
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| POST /api/driver/trips/log ownership | integration | tests/integration/driver-trip.test.js | a rider, an unassigned driver, a driver logging under another driver's id, and an unrelated manager are all refused 403; the vehicle's assigned driver, its managing manager, and a super-admin can each log a trip | trip-log ownership rule changes |

## Manager and Super Admin
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| /api/manager/* | integration | tests/integration/admin/manager.test.js | dashboards, bus updates, requests | manager workflow changes |
| /api/super-admin/* | integration | tests/integration/admin/super-admin.test.js | dashboards, reviews, audits, managers | admin workflow changes |
| POST /api/manager/drivers, identity | integration | tests/integration/manager-drivers.test.js | every driver gets a permanent `driverCode`; email is optional (field left unset, not blank) and several email-less drivers can coexist under the sparse unique index; clearing an email on update frees it for reuse; password < 8 chars and malformed emails → 400 | driver identity fields, the email index, or create validation change |
| POST /api/manager/drivers, organization | integration | tests/integration/manager-drivers.test.js | links an existing organization; creates one inline from name + category; missing category → 400; unknown id → 400; none given → null; GET/POST /api/manager/organizations list and create, filtered by category | organization resolution or the manager organization endpoints change |
| POST /api/manager/drivers, vehicle number | integration | tests/integration/manager-drivers.test.js | assigns by vehicle ID or number plate (any case); a plate matches however it was spaced ("pf- 2327" finds "PF-2327") and with a province ("wpcab1234" finds "WP CAB-1234"); reports in the message when the vehicle moves off its previous driver; unknown number → 400 and no driver created; another manager's vehicle → 400 | vehicle matching or assignment-on-create changes |
| phoneNumber utils: cleanPhoneInput, formatPhone, isValidPhone | unit (no DB) | tests/unit/phone-number.test.js | local (0771234567) and +94 forms accepted with any spacing or punctuation; cleanPhoneInput caps typing at ten digits, eleven behind a +; validation rejects an over-long number outright rather than truncating it into a valid one; nine digits, a missing leading zero and a non-94 country code rejected | the accepted phone formats change (keep in step with TrackMe-WebAdmin/src/lib/phone-number.js) |
| POST /api/manager/vehicle-accounts (creates outright) | integration | tests/integration/manager-vehicle-create.test.js | creates the Vehicle and its Driver immediately with no ManagerVehicleRequest raised; the driver gets managerId, a driverCode and an enrollment key; the vehicle is usable at once as a driver's vehicle number; driver email optional; an email on another manager's driver is refused without touching their password, while this manager's own driver is reused; duplicate vehicle ID or plate → 409; a failed Vehicle save leaves no orphan driver | vehicle creation moves back behind approval, or the created shape changes |
| Seat capacity retired from vehicle write paths | integration | tests/integration/manager-vehicle-create.test.js, tests/integration/bookings.test.js | POST /api/vehicle/register no longer requires seatCapacity (validator is `.optional()`); the manager create path and MANAGER_EDITABLE_FIELDS no longer accept it, so vehicles are stored with seatCapacity null; the field itself stays on the model because bookingController.getAvailableSeats builds its seat map from it (falling back to 45) and vehicleController exposes a totalCapacity aggregate | a UI starts collecting seat capacity again, or the booking seat map stops depending on it |
| Driver enrollment keys: reveal, rotate, revert | integration | tests/integration/manager-drivers.test.js | reveal returns the same key every time and reports `canRevert`; rotate issues a different key and stops the old one resolving via findDriverIdByEnrollmentKey; revert restores the superseded key and makes it resolve again; the undo works once per rotation (second attempt → 409), 409s when nothing was rotated, and reaches only the most recent key after two rotations; neither the current nor the superseded key is stored in plaintext; another manager's driver → 404 on reveal and revert | the key rotation/undo contract, the `previous` material on DriverEnrollmentKey, or enrollment lookup changes |
| PATCH /api/manager/vehicle-accounts/:vehicleId/reset-password | integration | tests/integration/reset-vehicle-account-password.test.js | a deliberately deactivated driver stays deactivated after this call (only isEmailVerified flips true, never isActive); the new password is still written and usable (login on the still-deactivated account 403s with the deactivation message, not 401 bad-credentials); an already-active driver stays active | issue #44 — resetVehicleAccountPassword's isActive/isEmailVerified side effects change |
| Phone validation on driver writes | integration | tests/integration/manager-drivers.test.js | POST /api/manager/drivers rejects a non-Sri-Lankan number and stores either the local or the +94 form as given; PUT rejects a malformed one | phone validation moves or the driver phone field changes |
| numberPlate utils: parsePlate, formatPlate, isValidPlate, plateMatches | unit (no DB) | tests/unit/number-plate.test.js | the three Sri Lankan shapes (CAB-1234, WP CAB-1234, 62-1234) canonicalised from any spacing or case; "WP-1234" read as a plate rather than a province prefix; wrong digit/letter counts and unknown province codes rejected; plateMatches falls back to a plain comparison for legacy rows | the accepted plate formats or the canonical stored form change |
| A plate belongs to one vehicle | integration | tests/integration/vehicle-plates.test.js | a plate already in the fleet, the same plate written differently, and the same registration under another province are all refused with a message naming the holding vehicle; another manager's plate is refused without naming their vehicle; a deleted vehicle's plate says so; creating past every controller guard still fails on the unique plateKey index | the plate identity rule changes, or a new endpoint writes plates |
| Number plate validation on vehicle writes | integration | tests/integration/vehicle-plates.test.js | POST /api/manager/vehicle-accounts rejects a malformed plate (wrong digit or letter count, unknown province) with the shared message and stores the canonical form, including pre-2000 numeric plates; PUT /api/manager/vehicles/:vehicleId rejects a bad plate leaving the old one in place, and canonicalises an accepted one | plate validation moves, or another endpoint starts accepting plates |
| PUT /api/manager/vehicles/:vehicleId — empty update + concurrent-write race | integration | tests/integration/vehicle-plates.test.js | an empty `{}` body is rejected with 400 and writes no `ManagerAuditLog` entry (issue #43); two updates racing onto the same brand-new plate always leave the loser with the same friendly 409 conflict message the non-race pre-check produces, never a raw duplicate-key error (issue #42) | the empty-update guard, or the plateConflict/duplicate-key handling on this endpoint changes |
| GET /api/driver/enrollment-key (driver's own) | integration | tests/integration/driver-enrollment-key-self.test.js | returns the same key the manager would reveal plus `isPrivate`; follows a rotation rather than serving the voided key; each driver gets their own and cannot name another (the id comes from the token); issues one on first read; passenger → 403, anonymous → 401 | the key contract changes, or drivers get another self-service account read |
| GET /api/vehicle/my-vehicle carries driver privacy | integration | tests/integration/driver-my-vehicle.test.js | the populated driver carries isPrivate true for a private driver and false (not omitted) for a public one, so the driver app can show whether enrolment needs approval; the widened projection still withholds the password and enrollment key; no vehicle → 404 | the driver app needs another driver field, or the my-vehicle projection changes |
| PATCH /vehicle-requests/:requestId/review, GET /vehicle-requests, GET /audit-logs — malformed ObjectId handling | integration | tests/integration/superadmin-id-validation.test.js | a malformed `requestId` param or `managerId` query on any of the three returns a clean 400 (not a 500 leaking the raw value); the vehicle-requests list still 200s with no `managerId` filter | issues #51/#52/#53 — the ObjectId validation on these params/query moves or is removed |
| idValidation utils: allRequestedIdsFound (used by PATCH /api/super-admin/managers/:managerId/assign-vehicles) | unit | tests/unit/idValidation.test.js | every requested id matched → true; a duplicated valid id in the request no longer produces a false-negative (`vehicles.length !== vehicleIds.length` coincidence check, issue #82); a genuinely missing id → false; compares ids by string so ObjectId vs string doesn't false-negative | the id-existence check for this or another bulk-assign endpoint changes |
| PATCH /vehicle-requests/:requestId/review — CREATE_VEHICLE_ACCOUNT field whitelist | integration | tests/integration/review-vehicle-request-whitelist.test.js | approving a CREATE_VEHICLE_ACCOUNT request only persists the vehicle-creation whitelist (vehicleId, vehicleName, numberPlate, registrationNumber, routeId, vehicleType, serviceType, bookingEnabled, seatCapacity, organization) from the manager-submitted payload — a field outside that list (e.g. maintenanceStatus) is dropped rather than written verbatim | issue #81 — the whitelist on reviewVehicleRequest's CREATE_VEHICLE_ACCOUNT branch changes |
| PATCH /vehicle-requests/:requestId/review — concurrent reviews of the same request | integration | tests/integration/review-vehicle-request-concurrency.test.js | two APPROVE calls for the same request, interleaved so the second runs to completion while the first is paused mid-flight, deterministically: the status:'PENDING' filter on the atomic claim means only one wins (200); the loser gets a clean 400 ("Request already reviewed") rather than racing into Vehicle.create(); exactly one Vehicle document results and the request ends APPROVED | issue #56 — the atomic-claim (status:'PENDING' filter) on reviewVehicleRequest's status transition changes |
| PATCH /vehicle-requests/:requestId/review — DELETE_VEHICLE approval deactivates the orphaned driver | integration | tests/integration/delete-vehicle-orphan-driver.test.js | approving a DELETE_VEHICLE request (via the real manager delete-request → super-admin review flow) deactivates the deleted vehicle's driver, not just the vehicle; a vehicle with no driver assigned approves cleanly with no error | issue #45 — the DELETE_VEHICLE branch's driver-deactivation side effect changes |
| POST /api/manager/vehicle-accounts bootstrap rule + PATCH /vehicle-requests/:requestId/review — CREATE_VEHICLE_ACCOUNT | integration | tests/integration/vehicle-create-approval.test.js | a manager's first vehicle is created immediately, with no request; every vehicle after that raises a PENDING CREATE_VEHICLE_ACCOUNT request instead (and a duplicate pending request for the same vehicle ID is refused); approving one creates the vehicle and a driver owned by the requesting manager (visible in their own directory); rejecting one leaves no vehicle behind; approving a request whose driver email was claimed by another manager after submission is refused and releases the claim back to PENDING; a manager (not a super admin) cannot review | the bootstrap threshold, the CREATE_VEHICLE_ACCOUNT approval branch, or its driver-ownership guard change |
| GET /api/super-admin/operations, GET /api/super-admin/vehicle-requests — pagination | integration | tests/integration/superadmin-operations-pagination.test.js | pagination is opt-in (same convention as vehicleController.getAllRoutes): no page/limit param keeps returning the full unbounded list with no `pagination` key, unchanged from before; passing page/limit paginates and returns `pagination` metadata; an oversized requested limit is clamped to 100 | issue #63 — either endpoint's pagination behavior or default (full-list) response shape changes |

## Custom Routes (School/Work Shuttles)
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| geo helpers: pointToSegmentMeters, minDistanceToPolylineMeters, deviationStats | unit | tests/integration/custom-route-geo.test.js | on-segment, off-segment, degenerate polyline/breadcrumb, off-route threshold | deviation math or off-route thresholds change |
| roadSnap: encode/decodePolyline, downsample, snapToRoads | unit | tests/integration/custom-route-road-snap.test.js | round-trip encoding, jitter downsampling, batching >100 pts, missing-key/API-error fallback | snap batching, fallback behavior, or polyline format changes |
| Custom-route provisioning, recording, naming, visibility | integration | tests/integration/custom-routes.test.js | CUSTOM request provisions a PRIVATE PENDING_NAMING route; POST /api/driver/custom-routes/record fills+snaps; ownership 404s; PATCH .../name activates; manager-scoped list/dropdown; PRIVATE route absent from /api/routes, /api/routes/:id, /api/bus/routes, /api/bus/route/:id, /api/bus/stops, /api/bus/routes/:id/path, and cross-manager bus updates | custom-route flow, provisioning, or visibility filtering changes |
| Off-route detection + resolve (Phase 2): report-journey, record-update, resolve | integration | tests/integration/route-change-requests.test.js | on-route journey not flagged; sustained off-route journey flags + creates exactly one RouteChangeRequest + notification; dedupe (no 2nd PENDING while one exists); record-update creates/updates a candidate with stops; resolve KEEP_OLD leaves geometry untouched, ADOPT_NEW overwrites pathPolyline/stops/distance; idempotent double-resolve; cross-manager 404; invalid resolution 400 | deviation thresholds, RouteChangeRequest lifecycle, or resolve logic changes |

## Private Routes (Room-Key / PIN)
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| roomKey utils: generateCode, encryptCode/decryptCode, lookupHash, verifyCode, generateUniqueRoomKey | unit | tests/integration/private-routes-roomkey.test.js | 6-digit format/range, encrypt/decrypt round-trip + tampered authTag, deterministic/differing hash, constant-time verify true/false, unique-key retry-on-collision + max-retries throw | room-key crypto or generation logic changes |
| Manager privacy toggle, reveal/rotate room-key (owner-only), PUBLIC<->PRIVATE flag-clearing | integration | tests/integration/private-routes.test.js | privatize auto-generates key; cross-manager 403 on privatize/reveal/rotate; PRIVATE->PUBLIC clears isHidden/joinApprovalRequired/roomKey | manager privacy endpoints or Route pre-save clearing hook change |
| POST /api/routes/join/verify (PIN + PIN-then-approval) | integration | tests/integration/private-routes.test.js | correct code grants ACTIVE membership (no approval); wrong code 403 + attempt increment; lockout 429 + retryAfter after ROOM_KEY_MAX_ATTEMPTS; hidden-route manual code entry (no routeId); approval-required correct code creates exactly one PENDING request (no membership, no duplicate on retry) | verify flow, throttle thresholds, or approval gating changes |
| Manager join-requests inbox + decision, members list + revoke | integration | tests/integration/private-routes.test.js | approve grants membership + notifies; reject notifies with no membership; idempotent double-approve; revoke flips membership to REVOKED + notifies | join-request/decision or revoke logic changes |
| Enforcement matrix: getAllRoutes locked stubs, hidden absence, bus/route + eta/route membership gating | integration | tests/integration/private-routes.test.js | unhidden PRIVATE route listed locked with no stops; hidden PRIVATE route absent; bus/route/:id empty for non-member vs populated for ACTIVE member; eta/route/:id/all-buses 403 for non-member vs 200 for ACTIVE member; getRouteById stays 404 for PRIVATE (member or not) | listing projection, membership-gated bus/ETA reads, or detail-endpoint scope changes |
| GET /api/routes/my-private, DELETE /:routeId/membership | integration | tests/integration/private-routes.test.js | lists ACTIVE-membership routes; leaving clears membership from the list | my-private / leave endpoints change |

## Driver Enrollment (private drivers)
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| POST /api/enrollments/redeem | integration | tests/integration/driver-enrollment.test.js | public driver key enrols straight to ACTIVE; private driver key creates PENDING only; repeat redeem is idempotent (409 when ACTIVE, 200 when PENDING); REJECTED may re-request without duplicating; unknown key, inactive driver and blank key all fail generically; 8 wrong keys throttle to 429 and a good key clears the counter | redeem rules, the privacy gate, or the attempt throttle change |
| GET /api/enrollments/mine, DELETE /api/enrollments/:id | integration | tests/integration/driver-enrollment.test.js | lists ACTIVE and PENDING only (REJECTED hidden); leaving drops the record and allows re-enrolling | passenger enrollment list or leave behaviour changes |
| Manager queue + decisions | integration | tests/integration/driver-enrollment.test.js | queue and count scoped to owned drivers; approve flips PENDING to ACTIVE; reject leaves the passenger unenrolled; deciding twice 409s; another manager gets 404 and cannot decide | manager enrollment-request endpoints or ownership scoping change |
| Driver summary shape on an enrolment | integration | tests/integration/driver-enrollment.test.js | `driver.vehicle.routeId` carries the route the driver runs (on redeem and in `/mine`) and is null when the vehicle has no route, so the passenger app can tell which route an enrolment covers; `driver.phoneNumber` is returned for an ACTIVE enrolment, withheld as null while PENDING, and released once the manager approves | the passenger-visible driver fields change, or the phone-number gate moves |
| POST /api/enrollments/riders/:riderId, GET /api/enrollments/mine, DELETE /api/enrollments/:id | integration | tests/integration/enrollment-rider-path.test.js | the rider-profile entry point writes `studentId` (not the deprecated `userId`) and that row is readable back through `/mine`; a private driver's PENDING request survives a manager approval that calls `enrollment.save()` (validates the full document — a row saved without `studentId` cannot pass); leaving removes an enrolment owned through a rider profile; another account cannot leave someone else's | the rider-profile write/read split, or which field is authoritative, changes again |

## Live Location (driver GO → enrolled riders + manager — docs/modules/REALTIME.md)
| Item | Test type | Test file | Cases covered | Update when |
|---|---|---|---|---|
| socketRateLimit | unit (no DB) | tests/unit/socket-rate-limit.test.js | per-socket/per-event sliding windows; events and sockets counted independently; allowance recovers after the window; a full 50-entry offline-buffer replay is never refused; disconnect forgets a socket's buckets | the per-event limits or window logic change |
| liveTracking pure helpers | unit (no DB) | tests/unit/live-tracking-helpers.test.js | `validCoord` range + NaN/Infinity + null-island rejection; `resolveRecordedAt` trusts a plausible device clock and falls back to server time outside a 5-minute tolerance | coordinate validation or clock-skew handling changes |
| GET /api/vehicle/:vehicleId/live, GET /api/manager/vehicles/live | integration | tests/integration/vehicle-live-endpoint.test.js | every status code for rider/manager/driver, incl. wrong-role and right-role-wrong-owner; a manager's fleet list excludes another manager's vehicles | REST authz or response shape changes |
| driver:start-tracking / driver:location / driver:stop-tracking / vehicle:subscribe / vehicle:unsubscribe | ws integration | tests/integration/ws/live-tracking.test.js | going live creates exactly one `VehicleLiveLocation` doc and a second fix overwrites it (not a trail); an enrolled rider and the owning manager receive `vehicle:update`; a rider not enrolled with the vehicle's driver, an enrolled rider watching a different driver's vehicle, and a non-owning manager are all refused; a replayed older fix ACKs `stale:true` without being stored or broadcast; out-of-range coordinates refused; a driver cannot broadcast for another driver's vehicle and a rider cannot broadcast at all; stopping marks the vehicle offline immediately; a dropped connection does **not** end the shift immediately, and a reconnecting driver re-adopts the session instead of being refused; a full offline-buffer-sized burst is never rate-limited into failure; leaving an ACTIVE enrolment while subscribed force-emits `vehicle:access-revoked`; hardened socket auth rejects a refresh token, a token for a deleted account, and a deactivated account | the live-location socket contract, authorization rules, or disconnect/grace/staleness behaviour change |
| household `student:` auto-join | ws integration | tests/integration/ws/household-socket.test.js | a PRIMARY connection auto-joins every household profile's `student:<id>` room, and still receives its own | unrelated to location — QR attendance's household join, unchanged |
| manager bus tracking | ws integration | tests/integration/ws/manager-track.test.js | join/leave/receive | manager role rules change |

Regenerate the auto-generated catalog (`npm run devkit:catalog` from the repo root) whenever a
test is added or renamed, so `tools/devkit/docs/TEST_CATALOG.md`'s gap report stays honest.
